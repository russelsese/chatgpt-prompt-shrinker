# Token Minimizer for ChatGPT

This Python script reduces the number of tokens in a given text before sending it to ChatGPT.  
It uses **spaCy** for text cleaning and **Sumy** for summarization.

## Setup

1. Clone or download this repository.  
2. Install dependencies:

```bash
pip install -r requirements.txt
# chatgpt-prompt-shrinker
